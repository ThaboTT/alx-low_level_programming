#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 * input2 - variable to store  string
 * Return: Always 0 (Success)
 */
int main(void)
{
	char input2[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", input2);

	return (0);
}
