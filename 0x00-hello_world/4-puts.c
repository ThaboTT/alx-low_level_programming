#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 * input1 - variable to store a string
 * Return: Always 0 (Success)
 */
int main(void)
{
	char input1[60];

	strcpy(input1, "\"Programming is like building a multilingual puzzle");

	puts(input1);

	return (0);
}
